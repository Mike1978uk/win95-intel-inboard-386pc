# Contributor input ledger

Who gave us what, whether it **actually paid off**, and **whether they've been told**.

**Why this exists:** people who give good technical input tend to give more of it when they learn it
landed. Reporting an outcome back — especially a *specific* one, with the evidence — costs minutes
and is the single cheapest way to keep expert help flowing. The failure mode is silent success: the
input works, gets absorbed, and the person never hears about it.

**Rule:** when a lead is verified or shipped, the "told?" column becomes a task, not a nicety. Credit
in the README/docs as well, but a direct reply is what actually closes the loop.

**On provenance tagging:** the `[PRIMARY]` / `[AI-SOURCED]` marks used across these docs are about
*where a claim came from*, not how much it's worth. A named person with relevant expertise gets
checked immediately and carefully — that's why Michal Necasek's suggestion was byte-verified within
the hour. Unsourced AI claims go in the "test cheaply, in cost order" pile. Human-sourced data has
been consistently the more reliable of the two on this project.

---

## Outstanding — owed a reply

| Contributor | What they gave | Status | Told? |
|---|---|---|---|
| **@andrew-hoffman** | Outcome of the T130B steer — issue **#19** | ⭐ **CLOSED, CONFIRMED ON THE REAL 5160 2026-09-06.** Adaptec's `T130.MPD` drives the real T130B and the real SCSI chain in protected mode: all six targets enumerate, `RMM.PDR` loads and never reaches `INITCOMPLETE`, 7 teardown stages started and closed, no `IOS.LOG`. Verified off the CF, not from the screen. His Gemini-tagged steer on #3 (*"Trantor T128/T130 might be supported because the SCSI port drivers came from NT 3.51"*) is why anyone looked for this driver at all; his emulated T130B boot proved the IOS stack accepts a 32-bit miniport, which became the control for #21 | ✅ **told 2026-09-06** on [#19](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/19#issuecomment-5558963779), crediting all three of his contributions and answering the DMA-0 question with the hardware result |
| **@andrew-hoffman** | Outcome of the SCSI-miniport steer and the T130B control — issue **#21** | ⭐ **CLOSED, CONFIRMED ON THE REAL 5160 2026-09-06.** `XTIDEMP.MPD` claims the boot disk, serves it in protected mode (`RMM` stands down, SCSIPORT + DiskTSD complete) and releases it cleanly at shutdown. Verified off the CF, not from the screen. Published byte-identical in `dist/xtide_mpd/` and `FIXES.md`, md5 `561fb45b598ef5985e5a803016321f76`. His T130B boot was the control that justified deleting our IOS layer; the IOS punt (#17) is why there was a driver to write; the `hdc` INF class got the binding right before install | ✅ **told 2026-09-06** on [#21](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/21#issuecomment-5555504007), with one correction owed to him — the forced-node theory is **moot, not answered** (the driver now reads its base from `AdapterSettings` and never touches the node). Also **asked him to test stride 1** on his own XT-IDE hardware, which nobody has ever executed, and **offered help with the driver he wants to write** (a `.MPD` is a plain PE, builds with DDK `ML /coff` + `LINK /SUBSYSTEM:NATIVE`, SCSIPORT owns polling). The real-hardware T130B result is now delivered too — see the #19 row below |
| **@andrew-hoffman** **[PRIMARY]** | 2026-09-03 on [#21](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/21): three sources for the IOS layer - a [retrocomputing.SE answer](https://retrocomputing.stackexchange.com/questions/14308/how-does-windows-9x-determine-which-disk-drivers-correspond-to-which-bios-disks) on how IOS matches drivers to BIOS disks; Microsoft's own **[I/O Supervisor Guide for Windows 9x/Me](https://web.archive.org/web/20060506200533id_/http://download.microsoft.com/download/winddk/doc/1.0/W982KMe/EN-US/IOS_Guide.exe)** (archive.org, originally a Microsoft download); and a second working miniport, [sdz-mods/O2-OZ777_W98](https://github.com/sdz-mods/O2-OZ777_W98), in C against the Win2000 DDK | **Lands on exactly the open question.** The same evening the shutdown hang was bisected to `ISP_INSERT_CALLDOWN`: Windows sends I/O down our calldown at teardown and our transport does not return. The IOS Guide is the authoritative document for that layer and this project does not have it - the DDK's own `STORAGE.DOC` says nothing about teardown request flow. The SE link bears on blocker 2 as well: whether C: can be taken from the real-mode mapper at all, given the XT-IDE option ROM hooks INT 13h and Windows treats such a device as unsafe by default | **Replied 2026-09-03** on [#21](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/21#issuecomment-5424): gave him the five-build bisect table, said plainly that what we lack is any documentation of WHAT IOS sends a port driver at `System_Exit` - the gap his Guide should fill - and recorded the two negative fixes and the retracted log-location claim so he is not misled by the previous status. Confirmed his boot-disk warning has held: every run was on a copy of the card, which is why five destructive-sounding builds cost nothing. **Neither link is reachable from the dev environment**, so the Guide still needs downloading by hand |
| **@andrew-hoffman** | 2026-08-29 on [#19](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/19): booted Win95 from the **emulated T130B SCSI** with 32-bit disk access and paging, no Device Manager errors; shared the 86Box image | ⭐ **Proves this machine's IOS stack accepts a 32-bit storage miniport** — the assumption the whole port-driver plan rested on. Image linked from [`resources_and_sources.md`](resources_and_sources.md) (too large for git). **Reproduced independently here 2026-09-05**: `T130.MPD` installed on our own `ibmxt_inboard386` bed loads, claims a SCSI disk and CD-ROM, mounts and writes a FAT16 volume, and shuts down cleanly | ✅ **told 2026-09-05** on [#21](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/21#issuecomment-5547786120). ⭐ **The real-hardware result landed 2026-09-06 and #19 is CLOSED** — told on [#19](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/19#issuecomment-5558963779) |
| **@andrew-hoffman** **[PRIMARY]** | 2026-09-04 on [#21](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/21): steer to abandon the hand-written IOS port driver and write a **SCSI miniport under `SCSIPORT.PDR`** instead | ⭐ **Adopted, on a measurement rather than on the argument.** Ran his own T130B result as a control first: a polling, no-IRQ miniport holds a written FAT16 volume through a clean Windows teardown on the same image our `.PDR` wedges on. SCSIPORT is itself an IOS port driver, so the teardown path, DCB lifecycle and volume lock are sound here and the fault is our code. The port discards ~1,500 lines of IOS glue and keeps ~1,120 lines of transport. Costing: [`docs/scsi_miniport_costing.md`](scsi_miniport_costing.md) | ✅ **told 2026-09-05** on [#21](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/21#issuecomment-5547786120), with two corrections owed to him: the IOS Guide's polling contract is real but **retired as the cause** of the hang (measured — our driver holds nothing at the wedge), and our reading of `BufferAccessScsiPortControlled` was wrong |
| **Michal Necasek** (OS/2 Museum) | Use the BIOS's own IRET at `F000:FF53` instead of injecting a stub at `0x3C0` | ✅ **Verified correct** — `CF` at offset `0x7F53` in both 1986 ROMs. Strictly better: no injected code, no assumption that INT F0h is unused | ✅ **replied by email 2026-08-23** — byte evidence from both 1986 ROMs, why the `0x3C0` stub was worse, and verification on both emulator and real hardware |
| **Michal Necasek** | Asked whether Win9x could never support the Inboard because the card couldn't hold enough RAM | ✅ **Answered 2026-08-23, and conceded plainly**: the 5 MB only exists because of a *modern reproduction* daughterboard (Stynx / Harrison Frazier, ParrotyError). The software obstacles were surmountable but were never the binding constraint — RAM was, and in 1995 it was not removable. Framing used: Win9x *could* have worked on an Inboard, not that anyone could have run it | ✅ same email |
| **@andrew-hoffman** | XT DMA page register is only 4 bits → the SB Pro DMA buffer must live under 1 MB (#5, 2026-08-20) | ✅ **CONFIRMED BY MEASUREMENT 2026-08-24.** A page-register trace caught it live: `MSSBLST.VXD` allocates at `0x4E0000` (4.875 MB), the 4-bit latch truncates to `0x0E0000` (896 KB, adapter ROM) — the card plays ROM contents. He called it from the hardware alone. Note his lead was **not** what caused the BSOD (that was our own corrupt VDMAD patch) and `DMABufferIn1MB` is **not** the fix — the lever is `_PageAllocate`'s `maxPhys` | ✅ **replied and closed 2026-08-24** on [#5](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/5#issuecomment-5393237068). Fix deployed and confirmed on the real 5160 — `maxPhys` `0xFFF` → `0xFF`, page `0x4E` → `0x09`, clean audio. Told him his parenthetical ("or really first 640k") was the stricter and more accurate half, and that `DMABufferIn1MB` was my wrong call, not his |
| **@andrew-hoffman** | Follow-up 2026-08-23: primary sources (os2museum ×2, martypc book) **and** the pointer at the 386MAX source | ✅ **Paid off twice over.** 386MAX's `MARK_XT` sets `DMA_MAX = 0A0000h` — the XT ceiling is **640 KB, not 1 MB**, stricter than we assumed; and it independently confirms port `0xA0` as the XT NMI register (plan step 5). Checking it also surfaced an emulator fidelity bug: `dma_at = is286` makes 86Box give the Inboard a full 8-bit DMA page register where real hardware latches 4 — a concrete mechanism for why the old `vmad` patch passed in emulation and failed on hardware | ✅ **replied 2026-08-23** on [#5](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/5#issuecomment-5385997588) — gave him the 640 KB `MARK_XT` evidence, the `dma_at` fidelity bug his pointer surfaced, and an explicit "nothing is fixed yet"; asked him to keep the leads coming |
| **@andrew-hoffman** | Follow-up 2026-08-24: could DMA buffers live between 640 KB and 1 MB, if the Inboard can map RAM into UMBs? Cites the 1987 installation guide's 64 KB EMS page frame and asks whether it can backfill the rest | ✅ **Right about the capability, and it argues the opposite way.** `$386.SYS`'s `vremap` maps arbitrary 4 KB pages anywhere in meg 0 — further than the manual suggests — but it does it with the **386 page tables** (`pd_addr`/`pt_addr`, `cr0=8000_0001h`, `int 6Fh "will have to modify the page table"`), which a bus master never consults. The 1988 code already knew: `maint_dmabank` tracks the lowest remapped bank on every `vremap`, and the INT 13h hook **stages** (bounces) any transfer reaching it. So RAM up there would be CPU-visible and DMA-invisible — this exact bug class | ✅ **replied 2026-08-24** on [#5](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/5#issuecomment-5396115327). Gave him the `vremap`/`dmabank` primary source and the measurement (post-fix buffer at page `0x09`; no UMBs configured on this machine at all). His question moves `maxPhys` `0x9F` from pedantry to the plan: `0xFF` is only safe because VMM has nothing allocatable in `0xA0`–`0xFF` |
| **@andrew-hoffman** | 2026-08-25 on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3): "I don't think the 20 bit DMA range limit patch has made it to upstream 86box yet." | ✅ **Correct, and a real gap.** `dma_force_xt` reached 86Box in #7626, but only as an AT/XT *detection* override. `dma_page_is_xt()` and the `dma[addr].page = val & 0x0f` truncation did **not** — confirmed by diffing `86box_upstream/src/dma.c` against `86box_full/src/dma.c`. Upstream still hands guests 24-bit DMA reach on 20-bit hardware, so his own original bug class is invisible in emulation upstream. Not Inboard-specific; correct for any PC/XT-class machine | ✅ **replied 2026-08-25** on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3#issuecomment-5414247347). Gave him the exact missing lines. **Superseded twice below**: the description was overstated (upstream *does* truncate, it just gates on `dma_at`), and the PR is now raised as [#7771](https://github.com/86Box/86Box/pull/7771) |
| **@andrew-hoffman** | 2026-08-25 on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3): adopt a variant of [Fabien Sanglard's `agent.md`](https://fabiensanglard.net/agent.md/index.html) in `CLAUDE.md`, especially short commit messages and PRs — "Let it ramble in the plan files but it's hard to read through the history as it is" | ✅ **Adopted in full.** `CLAUDE.md` rewritten: subjects 50/72, bodies ≤ 5 lines, "ramble in the plan files, not in the history". Also became the `repo-hygiene` skill. Knock-on tidy: `git status` 93 entries → 0, seven session-scratch files archived, README credits cut to two lines per person, issue bodies given status headers, and a flat [what worked / what didn't](what_worked_and_what_didnt.md) page added | ✅ **replied 2026-08-25** on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3#issuecomment-5414247347), crediting him and Sanglard in `CLAUDE.md` and the skill itself |
| **@andrew-hoffman** | 2026-08-25 on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3#issuecomment-5414257699): add a `.gitattributes` pinning line endings, then `git add --renormalize .` and one normalising commit. Reference: [his WDMHDA file](https://github.com/andrew-hoffman/WDMHDA/blob/main/.gitattributes) | ✅ **Adopted, and it caught a live latent bug.** `tools/*.sh` and `vxd-patches/deploy_premonolith.sh` were LF by accident — with `core.autocrlf=true` and no rules, **any Windows clone got CRLF copies bash refuses to run**, and those are the scripts that write patched files to a real CF card. Also pinned every patch binary (`*.VXD/.DRV/.SYS/.PDR/.386/.COM`, `roms/**`) explicitly rather than trusting git's NUL-byte heuristic. **Two deviations:** `86box_full/` left under upstream 86Box's own `.gitattributes` so our tree stays diffable against master; and the normalising commit was **a no-op** here — autocrlf had already kept the database LF-clean | ✅ **replied 2026-08-25** on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3#issuecomment-5414315383). Gave him both deviations, the `git diff --cached --numstat \| awk '$1=="-" && $2=="-"'` safety check for binaries, and the directory-macro trap that marked six `roms/` READMEs binary in my first draft. Now in the `repo-hygiene` skill, credited |
| **Andrew's floppy question** (#3, 2026-08-25) | How was HD floppy support added on the real hardware? And: Windows hangs in file dialogs if the BIOS reports drives with no controller behind them — try Drive Letter Manipulator | ✅ **Answered by the project owner** (Sergey Kiselev ISA floppy/serial card + his BIOS ROM, driving a Teac FD-505: 1.44 MB 3.5" as A:, 1.2 MB 5.25" as B:). His hang diagnosis was close to the mark but inverted: the BIOS was reporting drives Windows had **no controller installed** for at all | ✅ **replied 2026-08-25** in the same comment. Told him the inversion and that `HSFLOP.PDR`'s `maxPhys` — his original finding, third occurrence — is what remains behind it |
| **@TC1995** | Mach8/Graphics Ultra EEPROM "power up configuration" angle (#7) — independently matched #4's own finding | 🟡 Still untested. Unblocking action remains `C-INFO.EXE` at a DOS prompt | 🟠 **partially** — referenced in the 2026-08-23 comment on #4, but not addressed to them directly and no result to report yet |
| **@TC1995** | Asked us to implement the Mach8 **RAM banks** so the self-test reports "Ok." rather than "RAM addressing" (#4) | 🟠 The emulator-side implementation is still not started, and is a *different* defect from the PIT delay-loop hack we ship — it would be a second upstream PR | ✅ **answered with data 2026-09-09** on [#8](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/8): first real-hardware register capture this project has taken — `SUBSYS_STAT 0x00AB`, `DISP_STAT 0x0001`, `GP_STAT 0x0000`, read over COMrade with the accelerator idle. **Next step is ours, not his**: read the same three ports in 86Box under the same conditions and diff. The 512 KB-vs-1 MB VRAM experiment belongs to this thread too — it is his subject, not Necasek's |
| **@TC1995** | 2026-08-25 on [#8](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/8#issuecomment-5412550427): "By RAM banks, I mean the Mach8 ones, not the VGA side ones, especially during the self-test procedure, it does some bitblt tests, then a rectangle fill test and a pixtrans read to return 0xaaaa, 0x5555, etc. (in 8514/A mode)." | ✅ **Correcting two wrong readings of mine.** Checked against source: `mach->bank_r`/`bank_w` (regs `0xB2`/`0xAE`) are used in exactly **one** place in `vid_ati_mach8.c` — feeding `svga->read_bank`/`write_bank`, the **VGA aperture**. The accelerator path (bitblt / rect fill / PIXTRANS) addresses `dev->vram` with **no bank applied**. Self-test drives the accelerator, so banks alias → "RAM Addressing". **Hypothesis, not traced** | ⏳ **replied 2026-08-25** on [#8](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/8#issuecomment-5415251550) with the code evidence and **four specific questions** (which register selects the accelerator bank; bank-relative vs windowed addressing; the self-test sequence; meaning of `0007`). Offered an I/O trace of `0x82E8`–`0xE2E8`. **Awaiting his answer — blocked on information, not effort** |
| **@andrew-hoffman** | — (correction owed to him, not input from him) | ⚠️ **I overstated the upstream gap and corrected it.** Upstream *does* truncate: `dma[addr].page = dma_at ? val : val & 0xf`. What is missing is that it gates on `dma_at` (= `is286`), so an XT board with a 386 on it is handed 8 bits. Fix is **one line** — gate on `dma_force_xt \|\| !dma_at` | ✅ **corrected 2026-08-25** on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3#issuecomment-5415254386). Told him plainly the PR is still not written rather than implying otherwise |
| **@andrew-hoffman** | Outcome of his 20-bit DMA lead, emulator side | ✅ **SHIPPED UPSTREAM.** [86Box/86Box#7771](https://github.com/86Box/86Box/pull/7771) raised 2026-08-25, +14 −2 in `src/dma.c`: gate the page-register truncation on `dma_force_xt \|\| !dma_at` rather than `dma_at` alone. Built clean at upstream `2f6fff3`. PR states explicitly what is proven (equivalence for non-forcing machines, by inspection; the predicate already validated on real hardware) and what is **not** (no boot regression matrix across other XT/AT machines). His sources cited: os2museum ×2, MartyPC book, 386MAX `MARK_XT` | ✅ **told 2026-08-25** on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3#issuecomment-5415429893). Four suggestions from him, all four now acted on |
| **@andrew-hoffman** | 2026-08-25 on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3#issuecomment-5413531483): Windows 95 can use **real-mode** BIOS access for disks instead of its 32-bit drivers, and *"If Windows is using real mode access for any disk drives there will be an error message in the Performance tab of the System control panel"* | ✅ **CONFIRMED BY MEASUREMENT 2026-08-25**, off the live CF. `BOOTLOG.TXT` has **zero** `hsflop` entries; `RMM.PDR` (Real Mode Mapper) loads and completes, `ESDI_506.PDR` never loads. Hard disk **and** floppy both run through real-mode BIOS. Consequence: our patched `HSFLOP.PDR` is **inert** — it is on the card, md5-verified, and never executes, so three probe runs previously written off as "invalid" were measuring a driver that never ran. He predicted this from the symptom alone | ✅ **told 2026-08-25** on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3#issuecomment-5416096776), with the BOOTLOG evidence and an admission that his Performance-tab test would have found it from the GUI in seconds. Recorded as **Technique 74** in the debug skill: prove a driver *loads* before measuring what it does |
| **@andrew-hoffman** | 2026-08-25 on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3#issuecomment-5416593925): XTIDE Universal BIOS has options that must be set for Win9x to detect the drive in the BDA and use its 32-bit driver, and they **default to Off on non-386 builds**. Plus: real-mode storage is a major cause of the slow boot | ✅ **ROOT CAUSE OF #3.** Confirmed against the XTIDE docs: the option is **"Remove other hard drives"**, default *"Yes on the 386-builds, No on all other builds"*; *"if no drive is detected there, [Windows] refuses to load and defaults to MS-DOS compatibility mode."* Card runs the **V20 (XT+)** build → option off → exactly our measured `RMM.PDR`-instead-of-`ESDI_506.PDR`. **Unifies two investigations**: the floppy was never a floppy bug, it is the same compatibility-mode fallback | ✅ **replied 2026-08-25** on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3#issuecomment-5416784888). Also told him our test VM used an MFM ST225, so we had been unable to reproduce it; 86Box bundles `ide_xtp.bin`, so it is now testable in emulation before touching the card |
| **@andrew-hoffman** | The Performance-tab test (#3): *"If Windows is using real mode access for any disk drives there will be an error message in the Performance tab"* | ⭐ **THIS SOLVED #3.** The tab reads **"`INBRDPC` in config.sys forces MS-DOS compatibility mode"** — Windows names the Inboard's own required driver as the cause. Not the XT-IDE, not a missing node. Explains system-wide real mode (CD/MO/Zip all absent from Device Manager yet working). His BDA theory was disproved by measurement (`0040:0075 = 01`), but his own remark — *"a BIOS so old it doesn't even support hard drives"* — is what explained why. His performance point is now the **main prize** | ✅ **told 2026-08-25** on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3#issuecomment-5417278117 ). Six verified contributions. Fix path: an INBRDPC **VxD** to remove the real-mode trigger |
| **@andrew-hoffman** | 2026-08-26 on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3#issuecomment-5421000000): `IOS.INI` carries a whitelist of drivers IOS will tolerate hooking INT 13h, and `INBRDPC.SYS` is not on it. Source he found: <https://support.dynabook.com/support/viewContentDetail?contentId=108303> | ⭐ **The first partial escape from compatibility mode, and he measured it himself in 86Box.** Whitelisting clears `unrecognized hooker of INT 13h` from `IOS.LOG`; *Add New Hardware* then installs a Standard Floppy Disk Controller and the Performance tab stops reporting real mode for A:/B:. Hard disks stay in compatibility mode, so it is **necessary but not sufficient** — his own words. Reframes the escape as **per-device, not all-or-nothing**, which is what makes a 32-bit SCSI path (#19) worth trying before the VxD | ✅ **replied and split 2026-08-27** on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3). Carried into **#17** as the leading next step; **not yet tried on the real 5160** |
| **@andrew-hoffman** | 2026-08-26 on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3): *"Was the 20 bit DMA patch to the floppy driver included in the v2 disk image?"* | ✅ **No — and that is very likely his bug.** v2 published 2026-08-24; the floppy patch committed 2026-08-25. He was testing stock `HSFLOP.PDR` (`maxPhys 0x1000`), so his *"disk is not formatted"* fits the 4-bit page latch truncating a buffer above 1 MB exactly. His question found a real publication gap on our side | ✅ **answered 2026-08-27** on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3) with the dates and both files. Tracked as **#18**. Untested — until his whitelist find, the driver never loaded at all |
| **@andrew-hoffman** | 2026-08-26 on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3): 86Box rounds the RAM size down to the nearest power of 2 on opening and OK-ing Settings — 5120 → 4096, 3072 → 2048. Build 9780, macOS ARM64 | ⭐ **Real 86Box bug, root-caused to one line.** `qt_settingsmachine.cpp` does `temp_mem_size &= ~(granularity - 1)` in two places. The mask aligns from **zero** rather than from `min`, so with `min=1024, step=2048` every valid size (1024/3072/5120) rounds to an invalid one — his two numbers reproduce exactly. Two other machines (`t1000`, `maz1016`) affected via non-power-of-2 granularity. **Not our machine definition** — that is correct and identical upstream. It was corrupting our own test runs too | ✅ **FIXED UPSTREAM 2026-08-28.** Filed as [86Box#7805](https://github.com/86Box/86Box/issues/7805), credited to him, with the arithmetic and a suggested fix; his own report was on a **closed** issue and effectively invisible. OBattler took the suggested five lines **verbatim** in `9ee5197` and closed it — it is now the tip of 86Box master, and is applied to `86box_full` too. **Told 2026-08-28** on [#14](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/14#issuecomment-5450903909), where it also supplies a candidate cause: both RAM sizes that ever produced POST 101 collapse to 2048 KB under the bug |
| **@andrew-hoffman** | 2026-08-25 on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3), tagged by him as a Gemini suggestion: Future Domain 8-bit and Trantor T128/T130 *"might be supported because the SCSI port drivers and 32 bit IO subsystem originally came from Windows NT 3.51"* | ⭐ **Right, and better than [AI-SOURCED] implied.** Measured against the actual install sources: Win95 **and** Win98 both ship `fd8xx.mpd`, an in-box protected-mode miniport for an **8-bit ISA** SCSI card — MS KB [Q132061](https://jeffpar.github.io/kbarchive/kb/132/Q132061/) behind it. Neither ships T128/T130. But Trantor/Adaptec published `T130.MPD` separately ([archive.org](https://archive.org/details/T130_EXE)), which answers his own *"there doesn't seem to be a 32-bit Win95 driver available"*. **Kills the "nothing 32-bit can drive storage here" assumption** — it holds for XT-IDE, not for SCSI | ✅ **told 2026-08-27** on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3). Tracked as **#19**, with his own warning recorded (he corrupted a VM image adding a SCSI disk to the emulated T130B). Untested |
| **@andrew-hoffman** | 2026-08-25 on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3#issuecomment-5417500000): *"this issue should be renamed or split because now we're two root causes deep away from the original problem in the title"* | ✅ **Done.** #3 closed; split into **#17** (compatibility mode / `INBRDPC.SYS`), **#18** (floppy DMA reach) and **#19** (T130B miniport). Closing note records honestly that the browse-button symptom is fixed **in the emulator only** and unconfirmed on the real 5160 | ✅ **replied 2026-08-27** on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3) |
| **@andrew-hoffman** | 2026-08-26 on [#3](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/3): *"You might need to save tokens by only actively prompting instead of letting Claude Code run in a loop and burn all it wants to and not always on the highest priority action items"* | ✅ **Adopted.** `CLAUDE.md` gained a *How to spend a session* section quoting him: prompt actively rather than open-ended; cheapest test first; read the machine before theorising; a negative result from an unverified run is not a result | ✅ **not yet told** — include in the next reply |
| **@andrew-hoffman** | 2026-08-29 on [#22](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/22): three scans of the IBM 5160 Technical Reference, plus [NuXT](https://github.com/monotech/NuXT) and [DubaiXTClone](https://github.com/spencer-uk/DubaiXTClone/) — *"full KiCAD schematics that probably can be parsed"* | ⭐ **Both leads paid, and they demoted our own finding from discovery to citation.** The Tech Ref's I/O Address Map lists **ranges**: 8237A `000-01F`, **8259A `020-03F`**, 8253 `040-05F`, 8255 `060-06F`, DMA page `080-09F` — eight 32-byte blocks, published since 1983. DubaiXTClone was the right call on parseability: its decoder nets are `XA5 XA6 XA7 XA8 XA9` + `~AEN` in, `~DMACS ~INTRCS ~T/CCS ~PPICS ~WRTDMAPGREG ~WRTNMIREG` out — **`XA0`-`XA4` never reach the decoder.** That is the mechanism behind every `0xAC` we measured. NuXT recorded as checked and unhelpful here (FE2010A ASIC, no discrete decoder). Upgrades the 86Box report: `pic.c`'s two-port map now contradicts IBM's published map, not just our board | ✅ **replied 2026-08-29** on [#22](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/22#issuecomment-5462293296), with both citations and the honest limit (pin-level wiring not traced). Written into `docs/xt_io_aliasing_gotcha.md` and Technique 75 |
| **@andrew-hoffman** | 2026-08-29 on [#22](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/22#issuecomment-5463931785): *"Why so much focus on the LS120 driver anyway? The DOS driver apparently works [...] and the LS120 is not very fast storage anyway limited to 300 kb/s at best."* | ⭐ **Correct, and the most useful thing said on that issue.** He and the owner reached the same conclusion independently within an hour. Six binary patches had gone into a convenience feature *after* a bisect had already proved which component was at fault. Parked. **One correction to his premise, itself worth having:** the DOS driver *was* blocking IOS completely - `Punting miniports because of unknown ASPI driver SD120PPD` refuses protected-mode miniports as a **class** (issue #17), which is what made #18 and #19 untestable. Now fixed by whitelisting `sd120ppd.sys`/`aspihdrm.sys` in `IOS.INI`, so the real-mode driver and a 32-bit miniport can coexist | ✅ **replied 2026-08-29** on [#22](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/22#issuecomment-5464129267) with the closing status, the eliminations, and the premise correction. Written up as **Technique 77** - cost the target once a bisect names it |
| **@andrew-hoffman** | 2026-08-29 on [#5](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/5#issuecomment-5462487587): patched drivers can be distributed properly - either repack the Win95 CABs ([betaarchive method](https://www.betaarchive.com/forum/viewtopic.php?t=29398)) or, easier, extract the needed files, rewrite the INF to reference the patched copies, and ship that as a Have Disk package | ✅ **Built, and the second half was the better route.** `dist/havedisk-sbpro/` installs the SB Pro and the patched `MSSBLST.VXD` in one step. The INF is not hand-written: sections are lifted **verbatim** from OSR1's `WAVE.INF` by following the references out of `[PNPB002_Device]` transitively (12 sections, exactly 3 files), so the install logic stays Microsoft's and only `[SourceDisksNames]`/`[SourceDisksFiles]` change. It even drags in the right resources for this machine (`IOConfig=220-22F`, `IRQConfig=5`, `DMAConfig=1`). **Untested** - built and internally consistent, never installed | ✅ **replied 2026-08-29** on [#5](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/5#issuecomment-5462565519), with the extraction method and the limit (works for file-loaded drivers; never for anything Setup folds into `VMM32.VXD` - the `BOOTLOG` test is `Loading Device = <path>` vs `Loading Vxd = <name>`). **Follow-up 2026-08-29**: owner's decision to host it as an *alternative* - the copy-over-the-top route is the one measured on hardware and stays the default ([comment](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/5#issuecomment-5462577859)) |
| **@andrew-hoffman** | 2026-08-29 on [#19](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/19): T130 binds DMA channel 0 in Device Manager — *"Hope it's not actually using that DMA channel because that should still be in use for the DRAM refresh"* | ✅ **Right to ask, and it is a real-hardware-only problem.** DMA 0 *is* DRAM refresh on a 5160 — the BIOS spins at `F000:ECAC` until channel 0's address counter moves, refusing to continue until refresh runs; the Inboard replaces the CPU, not the planar. But `T130.MPD` never programs the 8237 (PIO-only import set), so `DMAConfig=0` is a claim it never acts on. Adaptec agree: `T128.INF` has the line **commented out**. Also settles the owner's point that a jumpered no-IRQ card should be supported — `T128.INF` sets `Polling=1` **unconditionally**, same driver family, same 1995-03-02 build | ✅ **replied 2026-08-29** on [#19](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/19#issuecomment-5462301400). `T130-XT.INF` committed with both changes plus `DontLoadIfConflict` from the same sibling INF. ✅ **Confirmed on hardware 2026-09-06**: the machine POSTs, runs and shuts down with the driver loaded, so DRAM refresh is demonstrably intact. Told on [#19](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/19#issuecomment-5558963779) |
| **Michal Necasek** (OS/2 Museum) **[PRIMARY]** | [*The 8514/A Graphics Accelerator*](https://www.os2museum.com/wp/the-8514a-graphics-accelerator/), surfaced 2026-08-25 | ✅ **Reframed issue #8.** Two facts: the 8514/A is **not memory-mapped at all** (VRAM reachable only via I/O / `PIX_TRANS`), so the "banks" cannot be a VGA aperture and `svga->read_bank`/`write_bank` is the wrong layer; and memory is **plane-organised** with the engine processing **4 or 8 bits depending on 512 KB vs 1 MB VRAM** — which 86Box models via `config1 \|= 0x20` and the `dev->bpp` branches. Yields a **free untried experiment**: run the Mach8 at 512 KB. Also gave the best remaining source, the **C&T 82C480 datasheet (Aug 1991)** | ⏳ **owed a note, and not gated on anything.** Second time his writing has unblocked this project (after `F000:FF53`), and that is the whole content of the reply — what his article changed here. **Corrected 2026-09-10:** an earlier version of this row held the note back until the 512 KB experiment had a result. That experiment is @TC1995's subject, on #8, and Necasek is not waiting on it. Do not gate a thank-you on an unrelated measurement |
| **Nick (@zikolas)** **[PRIMARY]** | [`cfu1-win9x`](https://github.com/zikolas/cfu1-win9x) — a from-scratch Windows 95/98 VxD for the RATOC REX-CFU1, **MIT licensed**. `win/vxd/CFU1.ASM` registers an IOS port driver (`DRP_MISC_PD` + `IOS_Register` + ILB + AEP handler) and acts as its own TSD; `win/build.sh` builds a Win9x VxD with **JWasm + Open Watcom v2** on a modern host | ⭐ **Changes the feasibility of a 32-bit XT-IDE driver from speculative to plausible.** It is a working template for the exact layer we would need, plus a free modern toolchain. His own hardest problems come from *dynamic* registration and hot-plug, neither of which applies to a fixed XT-IDE controller — so our case is the easier one. Nothing built yet; see [`docs/win9x_port_driver_feasibility.md`](win9x_port_driver_feasibility.md) | ✅ **Told 2026-09-01; he is happy for the driver to be shared widely.** His driver was **load-bearing twice** on the night the XT-IDE port driver first worked on real hardware: `rr_sg`/`rw_sg` gave the logical scatter/gather layout (sector count first, buffer pointer second), which is what a corrupted root directory was traced to; and `sched_vol` gave the appy-time rule (`_SHELL_CallAtAppyTime` + `CAAFL_RING0`), without which publishing a volume killed the boot. Both were reached by reading a **working** driver after the vendor sample and the vendor docs had each misled us. Credited by name in `XTIDETR.ASM`, the runbook and issue #21. **MIT attribution to settle before any binary is distributed** |
| **@andrew-hoffman** **[PRIMARY]** | The miniport steer, outcome | ⭐ **IT WORKS, 2026-09-05.** `XTIDEMP.MPD` loads, claims the **boot disk** (`RMM.PDR` loads and never reaches `INITCOMPLETE`; `SCSIPORT`/`DiskTSD`/`VFAT`/`IFSMGR` do), survives 5.9 MB written through it byte-for-byte, and shuts Windows down cleanly — the exact failure that defeated the `.PDR` for four sessions. `RealModeInitialized` turned out not to be needed. Emulator bed only; the 5160 is untested | ⏳ **owed.** Owner's call: tell him with the write-up **after** hardware validation, not before |

---

## Closed out — loop already closed

| Contributor | What they gave | Outcome | Told? |
|---|---|---|---|
| **Michal Necasek** (OS/2 Museum) **[PRIMARY]** | 2026-09-07, by email: `F000:FF53` is not a quirk of our two ROMs but part of the PC architecture — IBM treated the `IRET` there as a compatibility requirement from the PC/XT on, and properly functioning clones carry it. Citation: [AMIBIOS 98 Technical Reference](https://bitsavers.org/pdf/americanMegatrends/MAN-BIOS98-TR_AMBIOS_98_Technical_Reference_19980501.pdf) (search `FFF53`). Also asked whether Intel shipped 386MAX with the Inboard | ⭐ **Strengthens the shipped fix without changing a byte.** `IVT68FIX` and 86Box PR #7749 pointed `INT 68h` at `F000:FF53` on the strength of the byte being `CF` in the 09MAY86 and 10JAN86 ROMs — true, but it left the fix looking ROM-specific. It is not. Recorded in [`resources_and_sources.md`](resources_and_sources.md). His 386MAX question: **yes** — `ILIM386.SYS`, the memory manager in the Inboard's own Intel bundle, is a Qualitas OEM build ([`386max_and_the_inboard.md`](386max_and_the_inboard.md)) | ✅ **replied by the project owner 2026-09-07**, same day: the `ILIM386.SYS` answer, the protected-mode driver work, `T130.MPD`, and the Inboard machine now in 86Box master. He also conceded the work plainly — third time his input has moved this project |
| **@andrew-hoffman** | 2026-08-29 on [#21](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/21): `xtidecd.zip` — an XT-IDE ATAPI CD-ROM driver for DOS, source included, only ever posted to the 86Box Discord | ⭐ **The only known working code for the card's 8-bit data latch.** Confirms 86Box's register map independently, and settles master/slave: `ATASelectDevice` writes the DEV bit then reads it back to prove the unit exists. Hosted at [`drivers/xtide_cdrom/`](../drivers/xtide_cdrom/). Also found by disassembly: the zip's two binaries differ and the `.asm` builds the broken one — silent data corruption for anyone who rebuilds it | ✅ **Told 2026-08-29** on [#21](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/21#issuecomment-5465026548), including the broken-binary warning. He then **named the author — @OBattler — and explained the two binaries came from two separate pinned posts**, which is why they differ |
| **@QuantumByteRider** | Reported POST 101 and the duplicate `(1988) i386SX` entry on 86Box PR #7626 | ✅ **Root-caused and fixed** — the 1982 ROM incompatibility. Duplicate entry had been fixed in-tree by someone else | ✅ closing comment on #7626 (2026-08-22) |
| **@Fenix770** | Reported #7638 — memory reported "BAD" regardless of `mem_size`; suggested dev-branching the machine | ✅ **Same 1982-ROM cause**; they were also on `i386dx`, hitting a second defect simultaneously | ✅ replied on #7638, asked for a re-test rather than leaving the dev-branch suggestion unanswered |
| **@Fenix770** | Follow-up 2026-08-23 (our issue #11): still no extended memory after the updates, “random Shadow RAM errors” — **and attached his VM as a zip** | ✅ **The attachment was the contribution.** Run directly, it root-caused the shadow-RAM failure: 86Box places the high BIOS-shadow alias at `0xF0000 + mem_size*1024`, but the driver targets a **fixed `0x5F0000`** — the two agree only at mem_size 5120, the size the original trace was taken on. He had already added `NODIAGS` himself, so his disk reproduced the *next* failure cleanly. It also exposed that two earlier in-house attempts at this question had recorded “no difference at any memory size” from runs that were stalled at the POST F1 prompt and had never booted DOS | ✅ **replied 2026-08-24** on [#11](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/11#issuecomment-5397684273). Gave him the full measurement, said plainly there is **no fix yet** and why the one-line change is not shippable, confirmed his `NODIAGS` config is the right one to stay on, and **warned him off `mem_size = 5120`** — the obvious-looking workaround is the one path that crashes the emulator |
| **@Fenix770** | 2026-08-28 on [86Box#7805](https://github.com/86Box/86Box/issues/7805#issuecomment-5447778583): "most programs fail to detect the **extended** memory correctly" — Qualitas ASQ reports VDISK/Weitek FPU, the HDC utility reports **64 MB**. Three screenshots | ⚠️ **Probably expected XT behaviour, not a bug — unverified.** A 5160 has **no CMOS** and the Inboard machine correctly attaches no RTC; those utilities read extended size from CMOS `0x17`/`0x18`, and 86Box returns `0xff` for unmapped ports (`src/io.c:335`), so the two bytes read `0xFFFF` KB ≈ **64 MB** — exactly his figure. More broadly, extended memory here is provided by `INBRDPC.SYS`, not by CMOS or `INT 15h AH=88h`, which an XT BIOS lacks — so AT-era memory utilities have no correct way to ask. **Conventional 640 KB is not implicated**, and his own wording is about extended memory. Not measured: the control is to run the same utilities on a **stock IBM XT** | ✅ **replied 2026-08-28** on [#7805](https://github.com/86Box/86Box/issues/7805#issuecomment-5451251670). His comment had **zero replies** and sat on a closed issue about the settings dialog — the same invisibility that swallowed his RAM-rounding report until we filed #7805. Reply states plainly that we are not 86Box maintainers, gives him the stock-XT control test, and routes any real finding to a fresh issue. **Also asked which build he is on** — his comment postdates the 08-25 memory fixes but his binary may not |
| **@OBattler** | The `mem_size` 3072/5120 granularity answer on #7638 | ✅ Correct — that part was expected behaviour, not the failure | ✅ acknowledged in the #7638 reply |
| **Al Williams** (Dr. Dobb's, Hackaday) | His real 1990 `a20()` code | ✅ **Ports match this project exactly** — saved as `docs/al_williams_inboard_a20_correspondence_2023.md` | ✅ credited; worth a further note if the A20 path ever produces a new finding |
| **Wim Osterholt** | "XT, AT and PS/2 I/O port addresses" (1994) | ✅ Already produced a lead — port `0x00A0` is the **NMI mask register** on an XT, and our Inboard device also claims `0xA0` | n/a — 1994 document, credited in README |
| **@andrew-hoffman** | 2026-09-06 on [#21](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/21#issuecomment-5566000000): three minuszerodegrees links for XT-IDE card type and stride, plus his own card's design files ([Blue Lava Systems XT-CF](https://github.com/Bluelavasystems/XT-IDE-CF-MINI)), and the conclusion that only the 2009-2012 VCF Rev 1.0 cards were stride 1 | ⭐ **Found a real defect in the shipped driver.** The two HTML pages he linked carry no register-level detail (recorded as a dead end); the **register map JPG** is decisive and shows **three** maps, not two. The *Hi-Speed* map — the **default on every Rev 2/3/4 card** — is an A3/A0 line swap, a permutation no stride can express, so `XTIDEMP.MPD` **cannot drive it at all**. `FIXES.md`, the driver README and the submission drafts all claimed a "stock XT-IDE card" was covered. Caught before anything was sent. **One correction to his conclusion, which helps:** Rev 2/3/4 have a *Compatibility* mode presenting the Rev 1 map, so stride 1 is reachable on a modern card with a jumper — no 2009 board needed. Written up in `docs/xtide_register_maps.md`, technique 100 | ✅ **replied 2026-09-07** on [#21](https://github.com/Mike1978uk/win95-intel-inboard-386pc/issues/21#issuecomment-5568039594) with the full table, both corrections — his and ours — and the #17 closure |

---

## Owed a reply — VOGONS

| who | what | why it matters | told? |
|---|---|---|---|
| **disruptor** | 2026-09-01, [VOGONS t=112093 reply 30](https://www.vogons.org/viewtopic.php?t=112093&start=20): *"Perhaps you can provide some source code too. Unfortuneately we do not have an Inboard XT here too but it would be interesting if there is a possibility to add support for a ST01 SCSI controller too."* A colleague runs an ST01 with a 1 GB disk in a turbo XT; **the card uses ISA DMA** and has no ASPI driver | ✅ Wording read back 2026-09-10. The miniport template applies to the plumbing — SCSIPORT registration, the INF, the `ML /coff` + DDK `LINK` toolchain — and a Win9x miniport needs no ASPI layer at all. **But the ST01 transfers by ISA DMA, and ours are all PIO**, so the template stops exactly where their card starts | ✅ **replied** (reply 31): source is published, `XTIDEMP.MPD` linked, LS-120 and ST01 noted as in progress. ⚠ **One thing still owed** — the DMA-reach warning (technique 62): on an XT-class machine the page latch is **4-bit, 20-bit reach, 1 MB**, and a buffer above that does not fault, it transfers against the wrong address. Silent wrong data. They will hit it and it will not look like a DMA bug |
| **red-ray** | same thread: asked for `SIV32L.exe` output, supplied `vcredist_60_x86.exe` + Tahoma, a **SIV32L V5.88 Beta-09** build, and the `-DBGCPU` capture procedure. Then told the owner plainly that his logs showed **the CPU cache was not working** | ⭐ **HE WAS RIGHT, AND IT IS NOW FIXED.** His measurement is the only reason anyone looked. Captured 2026-09-12 over COMrade: the cache *was* enabled (`1000h:0 = 92`, CE + ASNP) but **`CMLR` (`1001h:4`) read `00`**, so nothing between 1 MB and 16 MB was cacheable while Windows 95 lives entirely above 1 MB — enabled, and doing nothing. Reference value `F0` decoded from REVTO486's dump on the 3.11 machine, applied to `CPUSET.BAT`, and **verified by read-back on the real 5160**. Write-up: [`cpu_cache_cmlr_2026_09_12.md`](cpu_cache_cmlr_2026_09_12.md). Note for him: SIV's own debug log says *"Use of the SIV Windows 9x VXD V5.88 rejected as it has not been written!"* and it reports `0.00MHz` with no TSC or CPUID — so on this machine it could not read CPU state directly and was inferring from timing. The inference was correct | ✅ **TOLD 2026-09-12** on the VOGONS thread — posted by the owner. Answered his actual question (*"are you 100% sure the L1 cache is enabled?"*): it was, and he was still right. Gave the four register read-backs, the `F0` reference value and its provenance, said plainly that the gain is **not yet benchmarked**, and noted for his benefit that SIV's own log reports its Win9x VxD as not written and 0.00 MHz with no TSC/CPUID — so it was inferring, and the inference was correct. ⭐ **BENCHMARK DELIVERED 2026-09-12**, both runs read by us: integer Dhrystone **2 -> 13-15**, elapsed 59.1 -> 36.1 s, and the latency walk now has a **24x cliff at exactly 16 KB** (0.325 -> 7.777 ms) where before 16 KB and 24 KB differed by 6% — his exact observation, answered in his own units. SIV also now resolves the CPU correctly (`486 DX2`, 16 KB L1) where before it guessed `486 DX`, 8 KB. One honest caveat for him: **everything above the L1 got ~2.3x slower**, probably line fills on a walk built to defeat reuse. [`cpu_cache_cmlr_2026_09_12.md`](cpu_cache_cmlr_2026_09_12.md). ⏳ **Owner to post** |

**Also raised by the owner:** rather than writing drivers to order, publish a **guide** — how to build a Win9x SCSI miniport for odd/8-bit hardware, using this project's own experience (`XTIDEMP.MPD` shipped and hardware-confirmed, `T130.MPD`'s INF adapted, `LS120MP.MPD` phase 0). Parked; lower priority than #22/#25.

## Foundational — credited, no open loop

| Contributor | Contribution |
|---|---|
| **SuperFury** (UniPCemu) | The entire Inboard 386/PC hardware model is a direct port of `hardware/inboard.c` |
| **Stynx & Harrison Frazier** (VCFed) | Designed the 4 MB Inboard daughterboard, without which Win95 wouldn't run at all |
| **CimonVg** | Ongoing work pushing the Inboard to its limits; inspiration and support |
| **RonnyRoy** | Reproducing the Inboard as cloned hardware — possibly the path past the 4 MB ceiling |
| **Kevin Moonlight** | Original author of [COMrade](https://github.com/yyzkevin/COMrade) | ⭐ **The validation path for this whole project, and decisively so on 2026-08-28.** The LS-120 keyboard root cause (#22) was found by static analysis but was only a theory until COMrade read ports `0x21`/`0x23`/`0x25`/`0x31`/`0x3F` on the live 5160 and returned `0xAC` for every one - proving the 8259 aliases across `0x20`-`0x3F`. No emulator could have shown it: 86Box does not model the alias. Also gave the negative result that the DMA page registers are write-only, which stopped a destructive test |
| **Ahmad Byagowi** | Ported COMrade to Windows 95 (`COMR95.EXE`) — the live real-hardware introspection path | ✅ **Load-bearing.** Reading files off the running machine (`IOS.LOG` direct from `C:\WINDOWS`) is how the storage state in #17 was measured without shuttling the CF, and how the LS-120 patch can be deployed in place rather than by reverting an image |
| **viti95** (FastDoom) | Real-hardware-validated XT keyboard ISR reference |
| **Microsoft Windows 95 DDK** | The genuine period source and toolchain behind the custom `VKD.VXD` |
| **[Miran Grča (@OBattler)](https://github.com/OBattler)** | Developer of 86Box, so the emulator this whole project is built on. Also wrote the **XT-IDE port of the ATAPI CD-ROM driver** now held at [`drivers/xtide_cdrom/`](../drivers/xtide_cdrom/) — the only known working code for the card's 8-bit data latch — and fixed [86Box#7805](https://github.com/86Box/86Box/issues/7805) from our report. **Not contacted by this project;** credited by name and link. Note his authorship of *both* the driver and `hdc_xtide.c` means they are not independent confirmations of the register map |

---

## Community write-ups relied on

| Contributor | What they gave | Where it landed |
|---|---|---|
| **Feipoa** (Vogons) | Extensive write-up on driving 486 upgrade-chip registers directly, and the pointer to the **CTCHIP / KTCHIP34** tool. Source: <https://www.vogons.org/viewtopic.php?t=45756> | **Issue #9, closed.** `CTCHIP34` from `AUTOEXEC.BAT` replaces `revto486.sys` entirely on the real 5160 + Inboard - confirmed booting to Windows, stable, and faster. Win95 had been running 1:1 clock with no cache at all; this recovered both the 2x multiplier and 640 KB of cacheable memory. The same post also carried the V86-mode clue that shaped the earlier Revto486 investigation |

Their account of ditching a vendor driver that "would even crash my machine" and going at the
registers directly with `KTChip34` is what made the register-level route look viable here at all.
Two things it saved us from: trusting `revto486.sys` on hardware it was never validated against, and
guessing at the register set - the values we shipped came from the real machine's own `CTCHIP34`
screens and were then independently corroborated by `REVTO486`'s own MSR dump.

**No reply owed — credit by reference is sufficient.** Project owner's decision, 2026-08-25:
Feipoa is not an active participant here, and this is a citation of a public write-up rather than
input given to this project. The credit stands in the README, this ledger and the debug skill; the
"tell them" rule below applies to people who engaged with us, not to every source we cite.

Attribution of post `p1392798` to Feipoa is per the project owner; the doc that recorded the quote
did not capture an author.

---

## When reporting back, include

What makes a reply worth sending — and worth replying *to*:

1. **The specific evidence.** "Verified — `CF` at file offset `0x7F53` in both 1986 ROMs" beats
   "you were right, thanks."
2. **What it replaced, and why theirs is better.** Shows the suggestion was understood, not just
   applied.
3. **Where it landed.** A PR or commit link makes it real and lets them see it in context.
4. **An honest negative result if that's what happened.** "We tried it, here's what we saw, it
   wasn't that" is genuinely useful to the person and keeps the exchange open. Silence after a
   failed suggestion is what stops people offering.
5. **Their full name or handle**, correctly.

## @andrew-hoffman, issue #23, 2026-09-10 — bus throughput and memory-mapped storage

| input | status |
|---|---|
| 32-bit miniports win on thunks and paging, **not** raw bus throughput; the raw win needs **every** storage driver 32-bit | **Accepted.** States the case for finishing #22 better than we had it. Recorded in `docs/bus_optimisation_plan.md` |
| Memory accesses likely have fewer wait states than I/O, so try memory-mapped storage (DiskOnChip, memory-mapped CF) | **CONFIRMED, with numbers.** Memory across the bus is **4.2x** faster per byte than our best I/O path (`rep insw`) and **12.7x** faster than byte-wide I/O. Measured on the 5160, `RMTMAF.OUT` |
| Caveat: the Inboard must not shadow the window such a card lives behind | **Accepted, and we know where it lives** — the `0x5E0000`/`0x5F0000` machinery, techniques 66/67/72 |
| DriveSpace has a 32-bit driver on Win95, above the miniports | **Corrects one of our notes.** We had parked compression because of its real-mode INT 13h hooker; that objection does not apply on the Windows path. Still parked, now for footprint and corruption risk |
| `rep insw`/`outsw` on every card that supports it | **Shipped for XT-IDE** (35% read / 33% write). **Not yet** for the Trantor T130B, which already imports the Ushort buffer calls |
| Extreme options: PAL equations, 8 MHz bus, Inboard redesign | Noted, out of scope |

**Owed:** a reply telling him the memory-vs-I/O hypothesis measured 4.2x and that the
DriveSpace note corrected a parked decision. Both are results he produced.

## @andrew-hoffman — consolidated OPEN items, swept 2026-09-11

Everything of his across #3, #18, #22, #23. Most is shipped (`.gitattributes` CRLF, the
`CLAUDE.md` writing rules, the 20-bit DMA fix upstreamed, the `IOS.INI` whitelist,
`HSFLOP-XT`). These three are not, and are the ones to work through.

### 1. `rep insw`/`outsw` on the Trantor T130B — ACTIONABLE NOW, no hardware needed

> *"Still makes sense to support REP INSW/OUTSW transfers on every type of card that it
> will work on for some easy free performance improvement."*  (#23)

Shipped for XT-IDE: **35% read / 33% write**. **Not** applied to the T130B, which already
imports `ScsiPortRead/WritePortBufferUshort` - so the miniport it ships with can already
do it and we have not checked whether it does. First step is
`python tools/pedis.py drivers/trantor_t130b/T130.MPD io` (technique 112), which prints
every port access with its width, and answers it from the binary.

⚠ On this bus a 16-bit I/O access splits into two 8-bit cycles (technique 109c), so the
win is **transaction count**, not width - the same reason "Mike's Hi-Speed mode" worked.

### 2. Issue #18 reproduction recipe — ACTIONABLE, and we have never run it

> *"The corruption and crash was on emulation, with the driver patched for DMA limit, when
> changing disks."* / *"I was using the Monster Floppy controller in 86box which includes
> the ROM for 1.4mb support."*  (#18, 2026-09-09)

That is a complete bed: **86Box + Monster Floppy controller + the DMA-patched driver +
a media change**. Our 2026-09-08 harness wrote twice to one disk and **never changed
media**, so it was not a re-test of the recorded trigger (technique 104). Building this
bed is the next step on #18 and needs no real hardware.

### 3. LS-120 throughput ceiling — a costing input we should hold ourselves to

> *"the LS120 is not very fast storage anyway limited to 300 kb/s at best"*  (#22)

Worth pinning against section 9 of `drivers/imation_ls120/TRANSPORT_SPEC.md`: the vendor
ships a 12x5 mode matrix and we implemented selector 0 of each. If his 300 KB/s is the
drive's own ceiling then the faster modes buy bus occupancy rather than throughput - which
is still the point of this track, but it changes how the result should be described.

**Owed on all three**, plus the two results from #23 already logged above (the 4.2x
memory-vs-I/O measurement, and the DriveSpace note correcting a parked decision).
