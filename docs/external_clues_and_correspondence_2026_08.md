# External clues and correspondence, August 2026 — raw capture

Verbatim-ish record of the source material behind
[`outstanding_work_plan_2026_08_23.md`](outstanding_work_plan_2026_08_23.md), kept separately so the
plan can be rewritten without losing the inputs.

**Provenance matters here.** Items are tagged:
- **[PRIMARY]** — a named person with relevant expertise, or a verifiable document
- **[AI-SOURCED]** — generated by an AI assistant, carries citation markers but no checkable source.
  Treat as hypotheses. Some claims in these sections are likely wrong.

---

## 1. Michal Necasek — email, Sun 9 Aug 2026, 19:12 **[PRIMARY]**

> Cool, thanks for letting me know! That's quite something. If I understand this correctly, back in
> the day Win9x could never support the Inboard 386/PC because there was not enough RAM to run Win9x
> in the first place?
>
> It's interesting but not totally surprising that the keyboard was the biggest stumbling block. The
> 8042 was baked into the PC/AT design and what Win9x did probably worked on just about every PC/AT
> and PS/2 class machine. So they had no reason to abstract the logic somehow.
>
> One very minor question... if you need an IRET instruction for an interrupt vector to point to,
> why not use the one at F000:FF53?

**Status:** the `F000:FF53` suggestion has been **verified correct** — byte is `CF` (IRET) in both
the 09MAY86 and 10JAN86 ROMs (file offset `0x7F53` in the U18/F800 chip). See plan item 0a. His
RAM question is still unanswered — see plan item 0b.

---

## 2. ATI Mach8 boot memory test — emulator vs real hardware **[AI-SOURCED]**

> The reason you are seeing a discrepancy between the emulator and your real hardware comes down to
> how the ATI Mach 8's dual-engine architecture (the standard 28800 VGA core and the 38800
> coprocessor core) interacts with memory initialization.
>
> **Why the test triggers in the emulator (but not on real hardware)**
> On real hardware, the card initializes in its standard text/VGA mode using its 512 KB of DRAM. The
> 1 MB of high-speed VRAM dedicated to the Mach 8 coprocessor sits isolated behind its I/O registers
> until a driver kicks it into accelerated 8514/A mode.
>
> Emulators (such as 86Box or PCem) handle video memory allocation fundamentally differently:
> - **The floating/uninitialized RAM trap:** many emulators pre-allocate or map the entire video
>   frame buffer area (including the VRAM) at startup, sometimes initializing that memory space to a
>   sequence of random bytes or non-zero patterns (0x00 vs 0xFF).
> - **The signature trigger:** the Mach 8 extension ROM contains self-test routines (ROM integrity,
>   register, and video memory POST sequences). On boot, the ROM checks specific hardware status
>   flags or registers. In the emulator, because the uninitialized VRAM state or the virtualized
>   co-processor registers return unexpected data patterns, the ROM assumes the hardware
>   configuration has changed or is corrupt, triggering the full interactive diagnostic cycle. On
>   real hardware, the registers cleanly reset to exact default cold-boot states, slipping right past
>   the test threshold.
>
> **How to bypass or short-circuit the test**
>
> 1. **Look for the "secret" EEPROM override.** The Graphics Ultra uses a configuration utility
>    (`INSTALL.EXE` or `SETMACH.EXE` from the original ATI utility disk) that writes configuration
>    profiles directly to an onboard EEPROM chip. Run the official ATI DOS configuration utility and
>    look for an option labelled "Advanced Diagnostics", "Video Test on Boot", or "Co-processor
>    Test". Toggling this off writes a bit to the EEPROM. If the emulator isn't saving changes to the
>    virtual EEPROM file cleanly, it will drop back to checking the RAM manually on every cycle.
> 2. **The Jumper 3 (MEM_SIZE) check.** If the emulator profile is set to a 512 KB card but loading a
>    1 MB ROM (or vice versa), the ROM will flag a size mismatch and run the test continuously to
>    isolate the "missing" banks.
> 3. **The hex patch fix.** Load the ROM image into a hex editor, search for the initialization
>    structure following the standard `55 AA` video ROM signature, locate the sub-routine calls for
>    the 8514-Ultra register/memory diagnostics, identify the conditional jump (`JZ`/`JNZ`) that
>    branches into the memory test screen when a register check fails, and NOP it out (`0x90`) or
>    invert it to short-circuit straight to the standard VGA boot vector.

⚠️ The "28800 VGA core + 38800 coprocessor" architecture description should be checked against the
ardent-tool reference below before being relied on.

### Reference material for the above
- <https://www.ardent-tool.com/video/ATI_8514_Ultra.html> — detailed Mach8 / 8514 Ultra docs
- **Wim Osterholt's "XT, AT and PS/2 I/O port addresses"** (1994) — Mach8 / 8514-A register
  ranges, plus the `(XT)` / `(XT only)` markings that distinguish this machine from an AT.
  Online: <https://wiki.preterhuman.net/XT,_AT_and_PS/2_I/O_port_addresses>
  Local copy: `C:\Users\lycet\OneDrive\Desktop\XT_project\inboard_files\win95_attempts_files\XT, AT and PS2 IO port addresses.txt`
  **Curated and annotated for this project in
  [`xt_io_port_reference_annotated.md`](xt_io_port_reference_annotated.md).**

---

## 3. Sound on an 8-bit bus **[AI-SOURCED, except where noted]**

> Getting stable audio under these conditions is tricky because Windows 95 VxDs heavily rely on
> 16-bit high IRQs (IRQ 9–15) and 16-bit DMA channels (DMA 5–7) for high-quality audio mixing.
> Because you are physically bound to an 8-bit bus with only IRQs 0–7 and DMA 1–3 available, standard
> Sound Blaster or Windows sound VxDs will routinely lock up trying to hit non-existent AT cascade
> registers.
>
> **The VxD trap:** with a standard Sound Blaster 16 or clone, the Windows 95 native drivers try to
> hook into protected-mode VxDs that expect an AT architecture.
>
> **The solution:** force Windows 95 to drop back to the real-mode/16-bit standard drivers. Force-
> install an older Windows 3.1 style `.drv` sound driver (Sound Blaster 1.5 or 2.0 8-bit) via
> `SYSTEM.INI` under the `[drivers]` section instead of letting Windows 95 handle it through a modern
> protected-mode VxD stack. This restricts the OS to simple, unmixed 8-bit audio loops that respect
> native 8-bit XT interrupt routing.

On the CT1600 specifically:

> The Sound Blaster Pro 2 (CT1600) is arguably the absolute best choice for this hardware footprint
> since it is natively a true 8-bit ISA card (despite the physical 16-bit layout, it only uses the
> 8-bit section for signals, barring some mixer/CD-ROM edge cases).
>
> Targeting `vmad.vxd` (Virtual DMA Device) is highly tactical. In Windows 95, `vmad.vxd` handles the
> virtualization of the DMA controllers. Because it assumes an AT architecture, it defaults to
> managing two cascaded 8237 DMA controllers (channels 0–3 and 4–7). On your IBM 5160, there is only
> one 8237 controller. If `vmad.vxd` attempts to read/write or virtualize registers for the secondary
> AT controller (ports `0xC0`–`0xDF`), or handles 16-bit DMA page registers inaccurately for an XT
> bus, it will instantly trigger a fatal exception or lock the system the moment an application
> initializes audio.
>
> Questions to answer when back at the bench:
> - Does the `vmad.vxd` patch handle the 8-bit DMA page register differences (XT uses `0x80`–`0x8F`,
>   while AT handles them differently)?

**Outcome: the `vmad.vxd` patch built on this theory FAILED on real hardware.** Evidence photo:
`C:\Users\lycet\OneDrive\Desktop\XT_project\photos\image0 (1).jpeg`

### The better lead — @andrew-hoffman **[PRIMARY]**

> The PC DMA controller only has a 4-bit page register so you will need to make sure any buffer the
> sound blaster DMAs from is in the first 1mb of RAM that the DMA controller can see (using whatever
> logical address that the memory physically on the motherboard maps to)

This fits the symptom better than the secondary-controller theory and has a documented, code-free
test: `SYSTEM.INI` `[386Enh]` → `DMABufferIn1MB=Yes`. See plan item 2a.

---

## 4. Revto486 — the V86-mode clue **[PRIMARY — vogons forum post]**

Source: <https://www.vogons.org/viewtopic.php?p=1392798#p1392798>

> Somehow my Model 30 really does not like the driver Kingston supplied. I guess it does some weird
> A20 line voodoo that only a Model 60 understands or some model specific BIOS calls so I ditched
> that as it would even crash my machine when the driver was loaded with the L1 cache and clock
> doubling disabled! So I had to get creative and used KTChip34 to control the SLC's registers
> directly which seemed to work. At least I could see them change and when I enabled the clock
> doubling register the machine would display as a 50MHz machine in Checkit for instance. Only its
> performance was still that of a 10MHz 286! Either the ASIC on the Kingston module must have some
> internal wait state logic as a fail safe that needs to be deactivated or the registers on the CPU
> itself are somehow "protected". I also saw a few posts that mentioned this safety feature but no
> one seemed to know how to circumvent it.
>
> So I was close to giving up but then I remembered that Evergreen also made some upgrades like
> these. I own one of them but mine doesn't feature an IBM SLC. It has a TI 486SXLC-40 but I
> remembered they also had a version with this exact nice IBM 486SLC2-50 CPU on it so I used
> Evergreen's Revto486 driver, `revto486.sys` and lo and behold: It worked! **But it only works when
> I make sure the CPU is in 386 protected mode before loading the Evergreen driver and DOS is running
> in virtual x86 mode.** I still have some performance tweaks that I need to do but it runs fastDooM
> rather nicely.

Open question recorded at the time: *"How can we do this with Win95?"* — but the DOS-level load
ordering should be confirmed to matter first. See plan item 2b.

---

## 5. COMrade for Windows 95 **[PRIMARY — build report]**

> Built and verified. Artifact: `dist/COMR95.EXE`
>
> On Windows 95:
> ```
> COMR95 /com1 /baud 115200
> ```
>
> It supports HELLO/status, text screen, desktop thumbnails, file read/write, directory listing,
> attributes, CRC hashing, and keyboard input. The bridge now exposes `desktop_screenshot`.
>
> Verified:
> - Win95-compatible PE32, subsystem 4.0
> - Only classic `KERNEL32`, `USER32`, `GDI32` imports
> - Live serial test over Wine: HELLO, screenshot, file transfer, hashing
> - Python tests: 69 passed
> - DOS/QEMU regression: 22/22 passed
>
> The build script is `build-win95.sh`; it prefers Open Watcom and has a CRT-free MinGW fallback.

Upstream: <https://github.com/ahmadexp/Open-Source-PC110/tree/main/Software/COMrade>
Original project: <https://github.com/yyzkevin/COMrade> (Kevin Moonlight)

**Three trees exist and are not interchangeable** — see the plan's finding D. Ahmad's fork is a
strict protocol superset of Kevin's (all 44 opcodes identical, +13 added). The MCP servers were
pointed at `RiderProjects\COMrade_Latest`, **an old path for this project**, which is why
`desktop_screenshot` would not have worked with the newly deployed `COMR95.EXE`.

**Already available locally — no fetch needed:**
```
C:\Users\lycet\RiderProjects\Open-Source-PC110\Software\COMrade\dist\COMRADE.EXE   (DOS)
C:\Users\lycet\RiderProjects\Open-Source-PC110\Software\COMrade\dist\COMR95.EXE    (Windows 95)
```

Both should sit at the root of `C:` on the CF card for easy location. See plan item 1a.

---

## 6. Standing instruction

> See other git issues comments also — we need to address open issues as appropriate and comment any
> when we identify leads / fixes. Any dead ends we can clean up.
